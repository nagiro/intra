<?php

/**
 * Base class that represents a row from the 'proveidors' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * 07/08/11 10:10:27
 *
 * @package    lib.model.om
 */
abstract class BaseProveidors extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ProveidorsPeer
	 */
	protected static $peer;

	/**
	 * The value for the proveidorid field.
	 * @var        int
	 */
	protected $proveidorid;

	/**
	 * The value for the nif field.
	 * @var        string
	 */
	protected $nif;

	/**
	 * The value for the nom field.
	 * @var        string
	 */
	protected $nom;

	/**
	 * The value for the telefon field.
	 * @var        string
	 */
	protected $telefon;

	/**
	 * The value for the ce field.
	 * @var        string
	 */
	protected $ce;

	/**
	 * The value for the cc field.
	 * @var        string
	 */
	protected $cc;

	/**
	 * The value for the cp field.
	 * @var        string
	 */
	protected $cp;

	/**
	 * The value for the adreca field.
	 * @var        string
	 */
	protected $adreca;

	/**
	 * The value for the alta field.
	 * @var        string
	 */
	protected $alta;

	/**
	 * The value for the ciutat field.
	 * @var        string
	 */
	protected $ciutat;

	/**
	 * The value for the site_id field.
	 * @var        int
	 */
	protected $site_id;

	/**
	 * @var        array Factures[] Collection to store aggregation of Factures objects.
	 */
	protected $collFacturess;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFacturess.
	 */
	private $lastFacturesCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'ProveidorsPeer';

	/**
	 * Get the [proveidorid] column value.
	 * 
	 * @return     int
	 */
	public function getProveidorid()
	{
		return $this->proveidorid;
	}

	/**
	 * Get the [nif] column value.
	 * 
	 * @return     string
	 */
	public function getNif()
	{
		return $this->nif;
	}

	/**
	 * Get the [nom] column value.
	 * 
	 * @return     string
	 */
	public function getNom()
	{
		return $this->nom;
	}

	/**
	 * Get the [telefon] column value.
	 * 
	 * @return     string
	 */
	public function getTelefon()
	{
		return $this->telefon;
	}

	/**
	 * Get the [ce] column value.
	 * 
	 * @return     string
	 */
	public function getCe()
	{
		return $this->ce;
	}

	/**
	 * Get the [cc] column value.
	 * 
	 * @return     string
	 */
	public function getCc()
	{
		return $this->cc;
	}

	/**
	 * Get the [cp] column value.
	 * 
	 * @return     string
	 */
	public function getCp()
	{
		return $this->cp;
	}

	/**
	 * Get the [adreca] column value.
	 * 
	 * @return     string
	 */
	public function getAdreca()
	{
		return $this->adreca;
	}

	/**
	 * Get the [optionally formatted] temporal [alta] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getAlta($format = 'Y-m-d')
	{
		if ($this->alta === null) {
			return null;
		}


		if ($this->alta === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->alta);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->alta, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [ciutat] column value.
	 * 
	 * @return     string
	 */
	public function getCiutat()
	{
		return $this->ciutat;
	}

	/**
	 * Get the [site_id] column value.
	 * 
	 * @return     int
	 */
	public function getSiteId()
	{
		return $this->site_id;
	}

	/**
	 * Set the value of [proveidorid] column.
	 * 
	 * @param      int $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setProveidorid($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->proveidorid !== $v) {
			$this->proveidorid = $v;
			$this->modifiedColumns[] = ProveidorsPeer::PROVEIDORID;
		}

		return $this;
	} // setProveidorid()

	/**
	 * Set the value of [nif] column.
	 * 
	 * @param      string $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setNif($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nif !== $v) {
			$this->nif = $v;
			$this->modifiedColumns[] = ProveidorsPeer::NIF;
		}

		return $this;
	} // setNif()

	/**
	 * Set the value of [nom] column.
	 * 
	 * @param      string $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setNom($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nom !== $v) {
			$this->nom = $v;
			$this->modifiedColumns[] = ProveidorsPeer::NOM;
		}

		return $this;
	} // setNom()

	/**
	 * Set the value of [telefon] column.
	 * 
	 * @param      string $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setTelefon($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->telefon !== $v) {
			$this->telefon = $v;
			$this->modifiedColumns[] = ProveidorsPeer::TELEFON;
		}

		return $this;
	} // setTelefon()

	/**
	 * Set the value of [ce] column.
	 * 
	 * @param      string $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setCe($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->ce !== $v) {
			$this->ce = $v;
			$this->modifiedColumns[] = ProveidorsPeer::CE;
		}

		return $this;
	} // setCe()

	/**
	 * Set the value of [cc] column.
	 * 
	 * @param      string $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setCc($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cc !== $v) {
			$this->cc = $v;
			$this->modifiedColumns[] = ProveidorsPeer::CC;
		}

		return $this;
	} // setCc()

	/**
	 * Set the value of [cp] column.
	 * 
	 * @param      string $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setCp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cp !== $v) {
			$this->cp = $v;
			$this->modifiedColumns[] = ProveidorsPeer::CP;
		}

		return $this;
	} // setCp()

	/**
	 * Set the value of [adreca] column.
	 * 
	 * @param      string $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setAdreca($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->adreca !== $v) {
			$this->adreca = $v;
			$this->modifiedColumns[] = ProveidorsPeer::ADRECA;
		}

		return $this;
	} // setAdreca()

	/**
	 * Sets the value of [alta] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setAlta($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->alta !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->alta !== null && $tmpDt = new DateTime($this->alta)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->alta = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = ProveidorsPeer::ALTA;
			}
		} // if either are not null

		return $this;
	} // setAlta()

	/**
	 * Set the value of [ciutat] column.
	 * 
	 * @param      string $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setCiutat($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->ciutat !== $v) {
			$this->ciutat = $v;
			$this->modifiedColumns[] = ProveidorsPeer::CIUTAT;
		}

		return $this;
	} // setCiutat()

	/**
	 * Set the value of [site_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Proveidors The current object (for fluent API support)
	 */
	public function setSiteId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->site_id !== $v) {
			$this->site_id = $v;
			$this->modifiedColumns[] = ProveidorsPeer::SITE_ID;
		}

		return $this;
	} // setSiteId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->proveidorid = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->nif = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->nom = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->telefon = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->ce = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->cc = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->cp = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->adreca = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->alta = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->ciutat = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->site_id = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 11; // 11 = ProveidorsPeer::NUM_COLUMNS - ProveidorsPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Proveidors object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ProveidorsPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = ProveidorsPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collFacturess = null;
			$this->lastFacturesCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ProveidorsPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseProveidors:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				ProveidorsPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseProveidors:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ProveidorsPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseProveidors:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseProveidors:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				ProveidorsPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = ProveidorsPeer::PROVEIDORID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ProveidorsPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setProveidorid($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += ProveidorsPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collFacturess !== null) {
				foreach ($this->collFacturess as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = ProveidorsPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collFacturess !== null) {
					foreach ($this->collFacturess as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ProveidorsPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getProveidorid();
				break;
			case 1:
				return $this->getNif();
				break;
			case 2:
				return $this->getNom();
				break;
			case 3:
				return $this->getTelefon();
				break;
			case 4:
				return $this->getCe();
				break;
			case 5:
				return $this->getCc();
				break;
			case 6:
				return $this->getCp();
				break;
			case 7:
				return $this->getAdreca();
				break;
			case 8:
				return $this->getAlta();
				break;
			case 9:
				return $this->getCiutat();
				break;
			case 10:
				return $this->getSiteId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = ProveidorsPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getProveidorid(),
			$keys[1] => $this->getNif(),
			$keys[2] => $this->getNom(),
			$keys[3] => $this->getTelefon(),
			$keys[4] => $this->getCe(),
			$keys[5] => $this->getCc(),
			$keys[6] => $this->getCp(),
			$keys[7] => $this->getAdreca(),
			$keys[8] => $this->getAlta(),
			$keys[9] => $this->getCiutat(),
			$keys[10] => $this->getSiteId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ProveidorsPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setProveidorid($value);
				break;
			case 1:
				$this->setNif($value);
				break;
			case 2:
				$this->setNom($value);
				break;
			case 3:
				$this->setTelefon($value);
				break;
			case 4:
				$this->setCe($value);
				break;
			case 5:
				$this->setCc($value);
				break;
			case 6:
				$this->setCp($value);
				break;
			case 7:
				$this->setAdreca($value);
				break;
			case 8:
				$this->setAlta($value);
				break;
			case 9:
				$this->setCiutat($value);
				break;
			case 10:
				$this->setSiteId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = ProveidorsPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setProveidorid($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setNif($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setNom($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setTelefon($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCe($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCc($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCp($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setAdreca($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setAlta($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setCiutat($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setSiteId($arr[$keys[10]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ProveidorsPeer::DATABASE_NAME);

		if ($this->isColumnModified(ProveidorsPeer::PROVEIDORID)) $criteria->add(ProveidorsPeer::PROVEIDORID, $this->proveidorid);
		if ($this->isColumnModified(ProveidorsPeer::NIF)) $criteria->add(ProveidorsPeer::NIF, $this->nif);
		if ($this->isColumnModified(ProveidorsPeer::NOM)) $criteria->add(ProveidorsPeer::NOM, $this->nom);
		if ($this->isColumnModified(ProveidorsPeer::TELEFON)) $criteria->add(ProveidorsPeer::TELEFON, $this->telefon);
		if ($this->isColumnModified(ProveidorsPeer::CE)) $criteria->add(ProveidorsPeer::CE, $this->ce);
		if ($this->isColumnModified(ProveidorsPeer::CC)) $criteria->add(ProveidorsPeer::CC, $this->cc);
		if ($this->isColumnModified(ProveidorsPeer::CP)) $criteria->add(ProveidorsPeer::CP, $this->cp);
		if ($this->isColumnModified(ProveidorsPeer::ADRECA)) $criteria->add(ProveidorsPeer::ADRECA, $this->adreca);
		if ($this->isColumnModified(ProveidorsPeer::ALTA)) $criteria->add(ProveidorsPeer::ALTA, $this->alta);
		if ($this->isColumnModified(ProveidorsPeer::CIUTAT)) $criteria->add(ProveidorsPeer::CIUTAT, $this->ciutat);
		if ($this->isColumnModified(ProveidorsPeer::SITE_ID)) $criteria->add(ProveidorsPeer::SITE_ID, $this->site_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ProveidorsPeer::DATABASE_NAME);

		$criteria->add(ProveidorsPeer::PROVEIDORID, $this->proveidorid);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getProveidorid();
	}

	/**
	 * Generic method to set the primary key (proveidorid column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setProveidorid($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Proveidors (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setNif($this->nif);

		$copyObj->setNom($this->nom);

		$copyObj->setTelefon($this->telefon);

		$copyObj->setCe($this->ce);

		$copyObj->setCc($this->cc);

		$copyObj->setCp($this->cp);

		$copyObj->setAdreca($this->adreca);

		$copyObj->setAlta($this->alta);

		$copyObj->setCiutat($this->ciutat);

		$copyObj->setSiteId($this->site_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getFacturess() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFactures($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setProveidorid(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Proveidors Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ProveidorsPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ProveidorsPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collFacturess collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFacturess()
	 */
	public function clearFacturess()
	{
		$this->collFacturess = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFacturess collection (array).
	 *
	 * By default this just sets the collFacturess collection to an empty array (like clearcollFacturess());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFacturess()
	{
		$this->collFacturess = array();
	}

	/**
	 * Gets an array of Factures objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Proveidors has previously been saved, it will retrieve
	 * related Facturess from storage. If this Proveidors is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Factures[]
	 * @throws     PropelException
	 */
	public function getFacturess($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProveidorsPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFacturess === null) {
			if ($this->isNew()) {
			   $this->collFacturess = array();
			} else {

				$criteria->add(FacturesPeer::PROVEIDORS_PROVEIDORID, $this->proveidorid);

				FacturesPeer::addSelectColumns($criteria);
				$this->collFacturess = FacturesPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FacturesPeer::PROVEIDORS_PROVEIDORID, $this->proveidorid);

				FacturesPeer::addSelectColumns($criteria);
				if (!isset($this->lastFacturesCriteria) || !$this->lastFacturesCriteria->equals($criteria)) {
					$this->collFacturess = FacturesPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFacturesCriteria = $criteria;
		return $this->collFacturess;
	}

	/**
	 * Returns the number of related Factures objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Factures objects.
	 * @throws     PropelException
	 */
	public function countFacturess(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProveidorsPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFacturess === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FacturesPeer::PROVEIDORS_PROVEIDORID, $this->proveidorid);

				$count = FacturesPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FacturesPeer::PROVEIDORS_PROVEIDORID, $this->proveidorid);

				if (!isset($this->lastFacturesCriteria) || !$this->lastFacturesCriteria->equals($criteria)) {
					$count = FacturesPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFacturess);
				}
			} else {
				$count = count($this->collFacturess);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Factures object to this object
	 * through the Factures foreign key attribute.
	 *
	 * @param      Factures $l Factures
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFactures(Factures $l)
	{
		if ($this->collFacturess === null) {
			$this->initFacturess();
		}
		if (!in_array($l, $this->collFacturess, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFacturess, $l);
			$l->setProveidors($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Proveidors is new, it will return
	 * an empty collection; or if this Proveidors has previously
	 * been saved, it will retrieve related Facturess from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Proveidors.
	 */
	public function getFacturessJoinConceptes($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProveidorsPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFacturess === null) {
			if ($this->isNew()) {
				$this->collFacturess = array();
			} else {

				$criteria->add(FacturesPeer::PROVEIDORS_PROVEIDORID, $this->proveidorid);

				$this->collFacturess = FacturesPeer::doSelectJoinConceptes($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FacturesPeer::PROVEIDORS_PROVEIDORID, $this->proveidorid);

			if (!isset($this->lastFacturesCriteria) || !$this->lastFacturesCriteria->equals($criteria)) {
				$this->collFacturess = FacturesPeer::doSelectJoinConceptes($criteria, $con, $join_behavior);
			}
		}
		$this->lastFacturesCriteria = $criteria;

		return $this->collFacturess;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Proveidors is new, it will return
	 * an empty collection; or if this Proveidors has previously
	 * been saved, it will retrieve related Facturess from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Proveidors.
	 */
	public function getFacturessJoinUsuaris($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ProveidorsPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFacturess === null) {
			if ($this->isNew()) {
				$this->collFacturess = array();
			} else {

				$criteria->add(FacturesPeer::PROVEIDORS_PROVEIDORID, $this->proveidorid);

				$this->collFacturess = FacturesPeer::doSelectJoinUsuaris($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FacturesPeer::PROVEIDORS_PROVEIDORID, $this->proveidorid);

			if (!isset($this->lastFacturesCriteria) || !$this->lastFacturesCriteria->equals($criteria)) {
				$this->collFacturess = FacturesPeer::doSelectJoinUsuaris($criteria, $con, $join_behavior);
			}
		}
		$this->lastFacturesCriteria = $criteria;

		return $this->collFacturess;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collFacturess) {
				foreach ((array) $this->collFacturess as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collFacturess = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseProveidors:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseProveidors::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseProveidors
